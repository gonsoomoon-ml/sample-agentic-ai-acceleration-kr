# Local settings — contains your account id. Copy from config.env.example.
config.env

# Runtime output: pre-change snapshots and rollback SQL generated by --apply.
# These hold live values from your database, so they stay out of git.
snapshots/
