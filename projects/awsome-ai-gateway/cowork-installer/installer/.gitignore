# Packaging build artifacts
.build-venv/
build/
dist/
wheels/
*.spec.bak
__pycache__/

# Build-time-injected corporate site defaults (contains env-specific OIDC ids).
# Generated by build.ps1; never commit — keeps environment identifiers out of VCS.
_site_config.py

# Site admin's edited copy of the baked-defaults input (guideline 1-1). Holds
# env-specific OIDC ids/domains; edit packaging/site-config.json, never commit.
site-config.json
packaging/site-config.json

# Build-time custom-key injection (guideline 1-4), copied next to cli/site_extra.py
# by build.ps1 from packaging/site-extra.json. Generated copy — never commit.
src/cli/site_extra.json
site_extra.json

